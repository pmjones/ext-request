{
    "name": "pmjones/ext-request",
    "description": "Server-side request and response objects.",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": "< 7.0.0"
    },
    "autoload": {
        "classmap": ["src/"]
    },
    "scripts": {
        "test": "TEST_PHP_EXECUTABLE=`which php` php ../run-tests.php ../tests -d auto_prepend_file=`pwd`/tests/prepend.php"
    }
}
